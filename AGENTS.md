# Agent notes

Cacheline is **free/local only**.

- Do not add paid APIs, API keys, purchases, or metered cloud services.
- Optional Redis (later day) is local Docker only.
- The HTTP demo (later day) may use a free public API such as JSONPlaceholder — nothing that requires an account or key.
- Keep weekday PRs scoped to the current `WEEK_PLAN.md` slice.
